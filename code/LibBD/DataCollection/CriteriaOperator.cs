namespace LibBD
{
    public enum CriteriaOperator
    {
        EQUALS, 
        NOT_EQUALS, 
        GREATER_THAN,
        LESSER_THAN, 
        GREAT_THAN_EQ,
        LESS_THAN_EQ,
        LIKE
    }
}